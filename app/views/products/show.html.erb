<p id="notice"><%= notice %></p>

<h2><%= @product.title %></h2>

<p>
  <%= image_tag(@product.image_url, :style => "height:150px;hspace:15px;vspace:15px;float:right;" ) %><%= @product.description %>
</p>


<p>
  <strong>Price:</strong>
  <%= number_to_currency(@product.price) %>
</p>


  <%= button_to 'Add to Cart', line_items_path( product_id: @product ) %><br>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back to Store', store_path %>
