<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>
<h2>Your Cart</h2>
<ul>
<% @cart.line_items.each do |item| %>
  <li><%= item.quantity %> &times;<%= link_to item.product.title, product_path(item)  %><%#= item.product.title %></li>
<% end %>
</ul>